# faststats.common

Not bound yet. The `faststats` wheel ships `faststats.neuro` only; `faststats.common` is planned as a thin slice of the `commonstats` crate. Crate guide: [`../rust/README.md`](../rust/README.md).
